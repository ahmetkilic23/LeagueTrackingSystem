@using MvcWebProjesi.Entity;
@model IEnumerable<Team>
@{
    ViewBag.Title = "Teams";
}

<main role="main">
    <!-- TOP SECTION -->
    <header class="grid">
        <div class="s-12 padding-2x">
            <h1 class="text-strong text-white text-center center text-size-60 text-uppercase margin-bottom-20">Teams</h1>
        </div>
    </header>

    <!-- SECTION 1 -->
    <section class="grid margin-bottom-20">
        <!-- Contact Information-->
        <div class="s-12 padding-2x background-white text-center">

            @*<form method="get">
                <select style="font-size:40px; margin-bottom:32px;" class="form-select form-select-lg">
                    @foreach (var item in Model)
                    {
                        <a href="'/Home/Players' + @item.Id">
                        <option value="@item.Id" name="team">@item.TeamName</option></a>
                    }

                </select>
                <button style="font-size:40px; margin-bottom:32px;" type="submit" class="btn btn-primary">Submit</button>
            </form>*@



            <table class="table table-dark">
                <thead>
                    <tr>
                        <th scope="col">id</th>
                        <th scope="col">Name</th>
                        <th scope="col">Country</th>
                        <th scope="col">Team</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <th scope="row">@item.Id</th>
                            <td>@item.TeamName</td>
                            <td>@item.Country</td>
                            <td><a href="/Home/@item.TeamName"><img src="@item.Image"></a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>


    </section>
</main>

